
CREATE TABLE users (
fb_user_id VARCHAR(100) NOT NULL PRIMARY KEY,
first_name VARCHAR(100),
last_name VARCHAR(100),
email_id VARCHAR(254),
phone_number VARCHAR(15),
current_auth_token VARCHAR(200)
);
